import { IntersectionCalculator } from './src/IntersectionCalculator'

const intersectionCalculator = new IntersectionCalculator;

intersectionCalculator.findIntersection(["1, 3, 4, 7, 13", "1, 2, 4, 13, 15"]);
